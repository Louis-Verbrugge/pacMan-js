
document.addEventListener("keydown", pressTouch)


function pressTouch(event) {
    alert(event.key)
    switch (event.key) {
        case "ArrowLeft":
            
            break;
        case "ArrowRight":
            break;
        case "ArrowUp":
            break;
        case "ArrowDown":
            break;
    }
}